services:
  - type: web
    name: doclink-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    rootDirectory: backend
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 10000
