To run the DocLink project, you need to start both the Backend (Express/Node) and the Frontend (Vite/React) servers.

Follow these steps:

1. Backend setup:
cd backend
npm install
npm run seed
npm run dev

2. Frontend setup:
cd frontend
npm install
npm run dev